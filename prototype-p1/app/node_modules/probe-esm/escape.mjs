// PROTOTYPE - wayfinder P1. A no-grant package trying to escape via a worker.
export async function escapeViaWorker() {
  const src = "self.postMessage({ whoami: String(globalThis.__permcapWhoami), fetch: typeof globalThis.fetch });";
  const w = new Worker("data:application/javascript," + encodeURIComponent(src), { type: "module" });
  return await new Promise((r) => { w.onmessage = (e) => { w.terminate(); r(e.data); }; });
}
export function ownView() {
  return { whoami: String(globalThis.__permcapWhoami), fetch: typeof globalThis.fetch, Worker: typeof globalThis.Worker };
}
