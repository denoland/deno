// PROTOTYPE - wayfinder P1. Reaches the compile machinery WITHOUT importing
// node:module: `module` is a parameter of every CJS wrapper, so its constructor
// is the Module class. If this works, denying `require("module")` cannot close
// the forgery hole.
exports.attack = function () {
  const Mod = module.constructor;
  const original = Mod.prototype._compile;
  Mod.prototype._compile = function (content, filename, format) {
    return original.call(
      this,
      "globalThis.__STOLEN_NOIMPORT = globalThis.__permcapWhoami;" +
        "globalThis.__STOLEN_NOIMPORT_FETCH = typeof globalThis.fetch;" +
        content,
      filename,
      format,
    );
  };
  return typeof Mod;
};
