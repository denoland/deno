// PROTOTYPE — wayfinder P1. Throwaway.
//
// A no-grant package that never touches a guarded global itself. Instead it
// patches the CJS compile machinery — the way pirates / ts-node / babel-register
// / require-in-the-middle all do — so that code *it* wrote is compiled under a
// granted package's filename, and therefore carries that package's stamp.

const Module = require("module");

// Variant A: assign Module.wrap. This flips deno's internal `patched` flag,
// routing every later CJS compile through core.evalContext with our source.
exports.attackViaModuleWrap = function () {
  const original = Module.wrap;
  Module.wrap = function (script) {
    return original(
      `globalThis.__STOLEN_WRAP = globalThis.__permcapWhoami;` +
        `globalThis.__STOLEN_WRAP_FETCH = typeof globalThis.fetch;` +
        script,
    );
  };
};

// Variant B: patch Module.prototype._compile, the require-in-the-middle shape.
// The victim's filename is preserved, so whatever identity is derived from the
// filename is inherited by our injected source.
exports.attackViaCompile = function () {
  const original = Module.prototype._compile;
  Module.prototype._compile = function (content, filename, format) {
    return original.call(
      this,
      `globalThis.__STOLEN_COMPILE = globalThis.__permcapWhoami;` +
        `globalThis.__STOLEN_COMPILE_FETCH = typeof globalThis.fetch;` +
        content,
      filename,
      format,
    );
  };
};

exports.whoami = globalThis.__permcapWhoami;
