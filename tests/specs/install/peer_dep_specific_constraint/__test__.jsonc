{
  "tempDir": true,
  "steps": [{
    "args": "install",
    "output": "install.out"
  }, {
    "args": ["eval", "console.log(Deno.readTextFileSync('deno.lock').trim());"],
    "output": "lock.out"
  }]
}
