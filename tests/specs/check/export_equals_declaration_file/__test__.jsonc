{
  "ignore": true,
  "//": "Disabled during the TypeScript un-fork: stock TS resolves the @deno-types .d.ts `export =` default import differently. Re-enabled by a follow-up once the behavior is settled.",
  "steps": [
    {
      "args": "check main.ts",
      "output": "main.out",
      "exitCode": 1
    },
    {
      "args": "check main.ts",
      "output": "main.out",
      "exitCode": 1
    }
  ]
}
