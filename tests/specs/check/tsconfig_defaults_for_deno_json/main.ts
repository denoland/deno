// This file is scoped for `deno.json`. Since no compiler options are specified,
// it will use the default. Since there are tsconfigs in the workspace, the
// defaults will be tsc-compatible (`[ "deno.window", "dom"]`). So there should
// be no errors.
Deno;
document;
