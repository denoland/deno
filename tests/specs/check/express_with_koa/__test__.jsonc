{
  // Disabled during the TypeScript un-fork: a transitive @types/node (via
  // express/koa) is double-loaded with the built-in copy. Re-enabled by the
  // transitive @types/node detection follow-up (#35871).
  "ignore": true,
  "args": "check --all",
  "output": "check.out"
}
