{
  "tempDir": true,
  "tests": {
    "pack_set_version": {
      "steps": [{
        "args": "pack --set-version 2.0.0",
        "output": "[WILDCARD]✓[WILDCARD]test-versioned-2.0.0.tgz[WILDCARD]"
      }, {
        "if": "unix",
        "commandName": "bash",
        "args": [
          "-c",
          "tar -xzf test-versioned-2.0.0.tgz && grep -q '\"version\": \"2.0.0\"' package/package.json && echo 'Has correct version: true' || echo 'Has correct version: false'"
        ],
        "output": "Has correct version: true\n"
      }]
    }
  }
}
