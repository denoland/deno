{
  "tempDir": true,
  "envs": {
    // Narrow the OS temp dir to under the cwd so the default temp write grant in
    // the compiled app does not cover the DENO_DIR path passed as the "outside"
    // argument (DENO_DIR sits in a sibling temp dir outside the cwd).
    "TMPDIR": "$PWD/tmp",
    "TMP": "$PWD/tmp",
    "TEMP": "$PWD/tmp"
  },
  "tests": {
    "write_confinement": {
      "steps": [
        {
          "if": "unix",
          "args": "compile --output write_confine_app main.ts",
          "output": "[WILDCARD]"
        },
        {
          "if": "unix",
          "commandName": "./write_confine_app",
          "args": ["$DENO_DIR"],
          "output": "run.out"
        },
        {
          "if": "windows",
          "args": "compile --output write_confine_app.exe main.ts",
          "output": "[WILDCARD]"
        },
        {
          "if": "windows",
          "commandName": "./write_confine_app.exe",
          "args": ["$DENO_DIR"],
          "output": "run.out"
        }
      ]
    }
  }
}
