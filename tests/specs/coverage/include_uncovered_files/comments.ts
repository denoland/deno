// A never-loaded file with nothing executable: only comments and blank lines.
// It must be skipped entirely (found_lines.is_empty()), so it never appears in
// the report below.

// (intentionally no code)
