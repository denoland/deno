{
  "tests": {
    "assertion": {
      // TODO(bartlomieju): disable import assertion support in TS.
      "ignore": true,
      "steps": [{
        "args": "run assertion.ts",
        "output": "assertion.out"
      }, {
        // should output the same because the emit won't be cached
        "args": "run assertion.ts",
        "output": "assertion.out"
      }]
    },
    "with": {
      "args": "run with.ts",
      "output": "with.out"
    }
  }
}
