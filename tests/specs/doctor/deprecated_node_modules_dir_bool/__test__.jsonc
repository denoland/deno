{
  "tempDir": true,
  "steps": [
    {
      "args": "doctor",
      "output": "report.out"
    },
    {
      // rewrites the deprecated boolean to the mode Deno derives from it
      "args": "doctor --fix",
      "output": "fix.out"
    },
    {
      "args": [
        "eval",
        "console.log(Deno.readTextFileSync('deno.json').trim())"
      ],
      "output": "deno.json.out"
    }
  ]
}
