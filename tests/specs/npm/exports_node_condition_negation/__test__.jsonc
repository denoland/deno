{
  "tempDir": true,
  "steps": [
    {
      // By default the "node" condition matches first and resolves the node
      // build (which would not register the webgpu backend).
      "args": "run --quiet main.js",
      "output": "node.out"
    },
    {
      // `!node` removes the "node" condition so resolution falls through to the
      // "import" (browser/webgpu) build. See denoland/deno#25397.
      "args": "run --quiet --conditions=!node main.js",
      "output": "browser.out"
    },
    {
      "args": "run --quiet main.js",
      "envs": {
        "DENO_CONDITIONS": "!node"
      },
      "output": "browser.out"
    },
    {
      // Negation is opt-in: adding unrelated conditions must not drop the
      // built-in "node" condition, so the node build still resolves.
      "args": "run --quiet --conditions=custom main.js",
      "output": "node.out"
    },
    {
      // Additions and removals can be combined in a single flag.
      "args": "run --quiet --conditions=custom,!node main.js",
      "output": "browser.out"
    }
  ]
}
