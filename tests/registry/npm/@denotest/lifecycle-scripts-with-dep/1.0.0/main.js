// Requires BOTH the file generated by the `postinstall` build script AND one of
// the package's runtime dependencies. The dependency is resolved through the
// `node_modules/<dep>` symlink the linker creates next to this package inside
// `.deno`. If a cached restore is missing either the generated file or the
// dependency link, this throws instead of printing the success line.
const generated = require("./generated.js");
const { add } = require("@denotest/add");
console.log(`${generated} with dep add(1, 2) = ${add(1, 2)}`);
